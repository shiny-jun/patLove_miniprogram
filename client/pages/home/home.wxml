<!-- pages/home/home.wxml -->
<view>
  <searchBar canuse="false"></searchBar>
  <current types="{{types}}">
    <block wx:for="{{types}}" wx:key="unique">
      <view slot="{{item.value}}">
        <view>1{{item.name}}</view>
      </view>
    </block>
  </current>
</view>