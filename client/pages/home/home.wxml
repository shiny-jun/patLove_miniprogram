<!-- pages/home/home.wxml -->
<text>
  <searchBar canuse="false"></searchBar>
  <current types="{{types}}">
    <text wx:for="{{types}}" wx:key="index" slot="{{item.value}}">
      <text>1{{item.name}}</text>
    </text>
  </current>
</text>